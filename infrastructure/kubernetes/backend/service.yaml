apiVersion: v1
kind: Service
metadata:
  name: formiq-backend
  namespace: formiq
spec:
  selector:
    app: formiq-backend
  ports:
  - port: 80
    targetPort: 8000
    protocol: TCP
    name: http
  type: ClusterIP 